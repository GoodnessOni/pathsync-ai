# PathSync AI — Backend Dependencies (Render-optimised, slim build)

# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9

# AI
anthropic==0.40.0

# Database
asyncpg==0.30.0
supabase==2.7.4

# PDF parsing
pdfplumber==0.11.4
PyMuPDF==1.24.10

# NLP / Privacy (lightweight only)
spacy==3.7.6

# Embeddings — OpenAI only (sentence-transformers removed — pulls 3GB torch)
openai==1.50.0
cohere==5.11.0

# Config
pydantic-settings==2.5.2
python-dotenv==1.0.1
